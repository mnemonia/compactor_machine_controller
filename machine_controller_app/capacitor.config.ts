import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'machine_controller_app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
